#include <stdio.h>

int main(){
	float a, b;
		printf("Enter a and b: ");
		scanf("%f%f", &a, &b);
			if(a > b){
				printf("float a > b");
			}
			else{
				printf("float a <= b");
			}
			return 0;
}
